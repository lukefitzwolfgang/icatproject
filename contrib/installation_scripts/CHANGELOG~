* v0.2.1rc1
	+ using mysql.connector for MySQL calls
	+ enabled python logger
	+ implemented error handling
	+ improved glassfish_setup_icat.sh
		+ target directory checks
		+ -y (yes) -t (test installation) -c (configure only) options added
	+ using "wget -c -q" (--continue and silent)
	+ various code cleaning and bugfixes
	+ added Vagrant file for quick testing (still experimental)

	
